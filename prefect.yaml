version: 3

deployments:
  - name: weekly-credit-risk-training
    entrypoint: src/pipelines/train_flow.py:training_flow
    work_pool:
      name: local-pool
    parameters:
      config_path: "configs/config.yaml"
    schedule:
      # Every Sunday at 15:00
      cron: "0 15 * * SUN"
      timezone: "Africa/Cairo"
